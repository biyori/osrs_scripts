This script loots bones and hides from Lumbridge cows at two different locations.
Each location changes randomly each time the script banks.

Also this script uses onLoop() instead of Tasks. I think onLoop is the best way to script.